import React, { useEffect } from "react";
import Loading from "@/components/Loading";
import { View } from "react-native";

const AppLoading = ({ loading }: any) => {
  useEffect(() => {

  },[]);

  return (
    <></>
  );
};

export default AppLoading;
